# PortView 环境变量配置

# ── JWT 密钥 (生产环境务必修改) ──
PORTVIEW_JWT_SECRET=change-me-in-production

# ── 默认管理员密码 (首次登录后可修改) ──
PORTVIEW_DEFAULT_PASSWORD=portview123

# ── 服务监听 ──
PORTVIEW_HOST=0.0.0.0
PORTVIEW_PORT=7577

# ── 配置目录 (SQLite + 自定义范围存储位置) ──
PORTVIEW_CONFIG_DIR=/app/config
